/** Shared evidence semantics keep notebook cards, Markdown and print in sync. */
export {
  deriveEvidenceThreads as deriveThreads,
  type HypothesisStatus,
  type ThreadInfo,
} from "./notebook-evidence-core";
