import type { RobustnessDraft, RobustnessPreview, RobustnessWorkflow } from "../lib/notebook-robustness";
export const robustnessDraft: RobustnessDraft = { title: "Sensitivity analysis", script: "analysis.py", inputs: ["data.csv"], metric: "difference", unit: "score", nullValue: 0, instance: "cpu-2", timeoutSec: 600, packages: [], specifications: [
  { key: "baseline", label: "Baseline", rationale: "Original model", seed: 42, parametersJson: '{"adjust":false}' },
  { key: "adjusted", label: "Adjusted", rationale: "Check confounding", seed: 42, parametersJson: '{"adjust":true}' },
] };
export const robustnessPreview: RobustnessPreview = { version: 1, projectId: "project-a", id: `rw_${"a".repeat(32)}`, digest: "d".repeat(64), source: { sessionId: "s1", entryId: "h1" }, sourceDigest: "s".repeat(64), planId: "plan-1", planRevision: 1, planHead: "head-1", draft: robustnessDraft, createdAt: 1000, expiresAt: Date.now() + 60000, inputFiles: [{ path: "data.csv", size: 4, sha256: "b".repeat(64) }], generatedFiles: [], scriptSource: "# exact reviewed Python\n", warnings: [], totalReservationUsd: 0.1, jobs: robustnessDraft.specifications.map((s, i) => ({ specificationKey: s.key, jobId: `mj_${String(i + 1).repeat(32)}`, command: `python3 analysis.py --spec ${s.key}.json --output result.json`, filesIn: ["data.csv"], outputPath: `robustness-results/test/${s.key}/result.json`, reservationUsd: 0.05 })) };
export const robustnessWorkflow: RobustnessWorkflow = { preview: robustnessPreview, approvedAt: 2000, cancelled: false, attempts: robustnessPreview.jobs.map((j, i) => ({ specification: robustnessDraft.specifications[i], jobId: j.jobId, state: "succeeded", outputPath: j.outputPath, resultStatus: "available", result: { schemaVersion: 1, metric: "difference", unit: "score", estimate: i === 0 ? 2 : -1, interval: { low: -2, high: 3, level: 0.95 }, qc: "pass" }, estimatedCostUsd: 0.01, reconciled: true })) };
